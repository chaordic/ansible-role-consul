---
- include: users.yml
  tags: consul-users

- include: dirs.yml
  tags: consul-dirs

- include: config.yml
  tags: consul-config

- include: install.yml
  tags: consul-install

- include: init.yml
  tags: consul-init

- include: services.yml
  when: consul_services | length > 0
  tags: consul-services

- include: backup.yml
  when:
    - consul_role == 'server'
    - consul_backup_enabled
  tags: consul-backup
